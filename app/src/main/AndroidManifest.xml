<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andre.monteiro.controlloan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DebugActivity" />
        <activity
            android:name=".TelaInicialActivity"
            android:theme="@style/AppTheme.Toolbar"
            android:parentActivityName=".TelaInicialActivity" />
        <activity android:name="com.example.andre.monteiro.contolloan.TelaEquipamentoActivity"
            android:theme="@style/AppTheme.Toolbar"
            android:parentActivityName=".TelaInicialActivity">
        </activity>
        <activity
            android:name="com.example.andre.monteiro.contolloan.EquipamentoActivity"
            android:theme="@style/AppTheme.Toolbar"
            android:parentActivityName="com.example.andre.monteiro.contolloan.TelaEquipamentoActivity" />

    </application>

</manifest>